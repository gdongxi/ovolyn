# internal strategy docs — never push to the public repo
HANDOFF.md
SUBMISSION_FORM_ANSWERS.md

# editor/agent tooling, not the project
.claude/

# secrets and local config
.env
.env.local

# dependencies and build output
node_modules/
.next/
dist/
*.tsbuildinfo
.vercel

# runtime state — the ledger and accounts belong to a deployment, not the repo
data/

# noise
.DS_Store
*.log

# recorded footage stays out; the recording harness itself is tracked
video/*.mp4
video/*.png
video/*.jpg
video/Ovolyn_Demo_video/
